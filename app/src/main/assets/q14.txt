对类的构造函数和析构函数描述正确的是( )。

构造函数可以重载，析构函数不能重载

构造函数不能重载，析构函数可以重载

构造函数可以重载，析构函数也可以重载

构造函数不能重载，析构函数也不能重裁

1

解析：一个类中只能定义一个析构函数，否则会造成对同一个对象的多次删除；而构造函数可以根据不同的参数个数和类型进行多次重载。

下列关于c++流的描述中，错误的是（　　）。

A．cout>>’A’表达式可输出字符A

B．eof()函数可以检测是否到达文件尾

C．对磁盘文件进行流操作时，必须包含头文件fstream

D．以ios_base：：0ut模式打开的文件不存在时，将自动建立一个新文件

1

解析：本题考查c++流，想要输出字符“A”，则应该是cout<<“A”，所以本题答案为A。

下列有关类成员的叙述中，正确的是( )。

A．友元函数是类的成员函数

B．类成员的默认访问权限是私有的

C．类成员函数必须声明为公有的

D．类的静态数据成员不能是常成员

2

解析：友元函数是指某些虽然不是类成员却能访问类的所有成员的函数，故友元函数不足类的成员函数。类成员的默认访问权限是私有的，类的成员函数¨r以是3种权限中的一种，静态数据成员可以为常成员。故本题答案为B
